spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stock_db?createDatabaseIfNotExist=true
    username: root
    password: Capg@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8088